# 2026-08-12

## WorkBuddy 平台适配

完成了 HuaweiCloud DevKit 对 WorkBuddy 平台的适配。

### 改动内容

1. **新建 `.workbuddy-plugin/plugin.json`** — WorkBuddy 插件清单，与 `.codex-plugin/` 等效
2. **修改 `setup-cli.mjs`** — 新增 `--target workbuddy` 安装/卸载/状态检查/自检功能
   - 安装路径：`~/.workbuddy/skills/`（Skills）、`~/.workbuddy/mcp.json`（MCP 配置）
   - 新增函数：`workbuddySkillsDir()`, `workbuddyMcpConfigFile()`, `workbuddyPluginsDir()`, `installWorkBuddy()`, `uninstallWorkBuddy()`, `workbuddyStatus()`
   - `parseTarget()` 新增 `workbuddy` 选项
   - `cmdDoctor()` 扩展了 WorkBuddy 路径检查
3. **修改 `tools.mjs`** — `resolveSkillsRoot()` 新增 WorkBuddy 技能目录搜索
4. **修改 `package.json`** — keywords 新增 `workbuddy`
5. **修改 `test/structure.test.mjs`** — 更新 codearts 测试断言，新增 workbuddy 端到端测试

### 使用方式

```bash
npx huaweicloud-devkit install --target workbuddy
npx huaweicloud-devkit doctor --target workbuddy
npx huaweicloud-devkit status --target workbuddy
npx huaweicloud-devkit uninstall --target workbuddy
```

### 验证结果

- 所有 72 个单元测试通过
- 6 个 workbuddy 集成测试通过（install / mcp.json / status / uninstall / preserve MCP / help）
- `npm run validate` 通过（27 个 skills 验证正常）

## 测试文件重命名

- `test/codearts-adaptation.test.mjs` → `test/cli-integration.test.mjs`
- 新增 6 个 workbuddy 端到端集成测试，覆盖 install / status / uninstall / MCP 配置 / 保留无关 MCP / help 文本
