# Huawei Core — Codex 插件系统设计文档

> **版本**: v1.0.0-draft  
> **模仿对象**: aws-core v1.1.0  
> **目标**: 让 Agent 通过自然语言完成华为云资源的调用和复杂任务的完成  
> **CLI 工具**: KooCli (hcloud)  
> **MCP 后端**: 自建 mcp-proxy-for-huawei (模仿 mcp-proxy-for-aws)

---

## 〇、设计原则

本文档严格按照 aws-core 的架构模式设计，遵循以下核心原则：

1. **多平台清单**：.codex-plugin / .claude-plugin / .cursor-plugin 三套共存
2. **技能驱动**：SKILL.md 是 Agent 行为的第一来源，不是 API 文档
3. **MCP 优先可降级**：MCP 提供沙箱+审计，不可用时降级到本地 KooCli
4. **安全钩子**：PreToolUse Hook 拦截密钥泄露
5. **重型数据外置**：references/ 存放 API 参数表、配额、模型 ID 等
6. **版本锁定**：关键依赖锁定版本号

---

## 一、顶层架构

`
agent-toolkit-for-huawei/
│
├── .agents/plugins/marketplace.json    ← Codex 市场清单
├── .claude-plugin/marketplace.json     ← Claude Code 市场清单
├── .cursor-plugin/marketplace.json     ← Cursor 市场清单
│
├── plugins/
│   └── huawei-core/          ← 当前唯一插件 (后续扩展: huawei-ai / huawei-data-analytics / huawei-devsecops)/      ← DevSecOps (对标 aws-agents-for-devsecops)
│
├── rules/                     ← 共享规则
├── setup-instructions/        ← 安装指引
├── tools/                     ← CI 脚本
│   ├── sync-plugin-skills.py
│   └── validate.py
│
└── 根配置文件
`

---

## 二、huawei-core 插件结构

`
plugins/huawei-core/
├── .codex-plugin/plugin.json     ← Codex 清单
├── .claude-plugin/plugin.json    ← Claude Code 清单
├── .cursor-plugin/plugin.json    ← Cursor 清单
├── .mcp.json                     ← MCP 服务器声明
├── hooks/                        ← 安全钩子
│   ├── hooks.json
│   └── secret-safety.py          ← DEW 密钥泄露拦截
├── skills/                       ← 技能目录 (20 个)
│   ├── huawei-ecs/               ← 弹性云服务器 (对标 aws-compute)
│   ├── huawei-obs/               ← 对象存储 (对标 S3)
│   ├── huawei-vpc/               ← 虚拟私有云 (对标 VPC)
│   ├── huawei-iam/               ← 统一身份认证 (对标 aws-iam)
│   ├── huawei-rds/               ← 关系型数据库 (对标 RDS)
│   ├── huawei-gaussdb/           ← GaussDB 分布式数据库 (对标 Aurora)
│   ├── huawei-functiongraph/     ← 函数工作流 (对标 Lambda)
│   ├── huawei-apig/              ← API 网关 (对标 API Gateway)
│   ├── huawei-cce/               ← 云容器引擎 (对标 ECS/EKS)
│   ├── huawei-smn-dms/           ← 消息通知+分布式消息 (对标 SNS/SQS)
│   ├── huawei-modelarts/         ← ModelArts AI 平台 (对标 Bedrock/SageMaker)
│   ├── huawei-cloud-eye/         ← 云监控 (对标 CloudWatch)
│   ├── huawei-cts/               ← 云审计 (对标 CloudTrail)
│   ├── huawei-dew/               ← 数据加密服务 (对标 KMS/Secrets Manager)
│   ├── huawei-billing/           ← 费用中心 (对标 Billing)
│   ├── huawei-cbr/               ← 云备份 (对标 Backup)
│   ├── huawei-waf-aad/           ← WAF+Anti-DDoS (对标 WAF/Shield)
│   ├── huawei-dds-dcs/           ← 文档数据库+缓存 (对标 DocumentDB/ElastiCache)
│   ├── huawei-deployment/        ← 部署(对标 CodePipeline/CodeDeploy)
│   └── huawei-getting-started/   ← 入门引导 (对标 launch/signing-in)
└── README.md
`

### 20 个技能对照表

| huawei-core 技能 | 对标 aws-core | 华为云服务 | KooCli 命令前缀 |
|---|---|---|---|
| huawei-ecs | aws-compute | ECS (弹性云服务器) | hcloud ecs |
| huawei-obs | S3 (在 aws-serverless 中) | OBS (对象存储服务) | hcloud obs |
| huawei-vpc | (在 aws-core 中隐式) | VPC (虚拟私有云) | hcloud vpc |
| huawei-iam | aws-iam | IAM (统一身份认证) | hcloud iam |
| huawei-rds | (在 aws-database 中) | RDS (关系型数据库) | hcloud rds |
| huawei-gaussdb | (在 aws-database 中) | GaussDB (分布式数据库) | hcloud gaussdb |
| huawei-functiongraph | aws-serverless (Lambda) | FunctionGraph (函数工作流) | hcloud fgs |
| huawei-apig | aws-serverless (API Gateway) | APIG (API 网关) | hcloud apig |
| huawei-cce | aws-containers | CCE (云容器引擎) | hcloud cce |
| huawei-smn-dms | aws-messaging-and-streaming | SMN (消息通知) + DMS (分布式消息) | hcloud smn / dms |
| huawei-modelarts | amazon-bedrock | ModelArts (AI 开发平台) | hcloud modelarts |
| huawei-cloud-eye | aws-observability | Cloud Eye (云监控) | hcloud ces |
| huawei-cts | 部分 aws-observability | CTS (云审计服务) | hcloud cts |
| huawei-dew | aws-secrets-manager | DEW (数据加密服务) | hcloud csms / kms |
| huawei-billing | aws-billing-and-cost-management | 费用中心 | hcloud billing |
| huawei-cbr | (在 aws-core 中隐式) | CBR (云备份) | hcloud cbr |
| huawei-waf-aad | (在 aws-core 中隐式) | WAF + AAD | hcloud waf / aad |
| huawei-dds-dcs | aws-database (路由) | DDS (文档数据库) + DCS (缓存) | hcloud dds / dcs |
| huawei-deployment | aws-deployment | (KooCli + CloudDeploy) | hcloud deploy |
| huawei-getting-started | signing-in-to-aws + launch-with-aws | 入门引导 | hcloud configure |

---

## 三、MCP 代理设计

### .mcp.json

\\\json
{
  \"mcpServers\": {
    \"huawei-mcp\": {
      \"command\": \"uvx\",
      \"args\": [
        \"mcp-proxy-for-huawei@1.0.0\",
        \"https://huawei-mcp.cn-south-1.myhuaweicloud.com/mcp\",
        \"--skip-auth\",
        \"--metadata\",
        \"INSTALL_SOURCE=agent-toolkit-huawei-core\"
      ]
    }
  }
}
\\\

### 架构图

\\\
┌──────────┐     stdio      ┌──────────────────────┐     HTTPS + AK/SK     ┌──────────────────────┐
│  Codex   │ ◄────────────► │ mcp-proxy-for-huawei │ ◄──────────────────► │ huawei-mcp           │
│  Agent   │   (MCP协议)    │     @1.0.0           │   (华为云签名)        │ 后端 (自建服务)       │
└──────────┘                └──────────────────────┘                      └──────────────────────┘
                                       │
                                       │ 读取本地凭证
                                       ▼
                              ~/.huaweicloud/credentials
                              (AK/SK 或临时 Token)
\\\

### mcp-proxy-for-huawei 功能清单

| 功能 | 说明 |
|------|------|
| 协议转换 | MCP JSON-RPC → KooCli 命令 或 华为云 REST API |
| AK/SK 签名 | 华为云签名算法 (SDK 内置) |
| 沙箱执行 | KooCli 命令在隔离环境中运行 |
| 审计日志 | 所有操作记录到后端 |
| 来源追踪 | INSTALL_SOURCE 元数据 |
| 凭证刷新 | 短期 Token 自动续期 |

---

## 四、安全钩子设计

### hooks.json

\\\json
{
  \"hooks\": {
    \"PreToolUse\": [
      {
        \"matcher\": \"Bash\",
        \"hooks\": [{
          \"type\": \"command\",
          \"command\": \"python3 \\\"\/hooks/secret-safety.py\\\"\",
          \"timeout\": 5
        }]
      },
      {
        \"matcher\": \"hcloud|mcp__huawei.*|mcp__plugin_.*huawei-mcp.*\",
        \"hooks\": [{
          \"type\": \"command\",
          \"command\": \"python3 \\\"\/hooks/secret-safety.py\\\"\",
          \"timeout\": 5
        }]
      }
    ]
  }
}
\\\

### secret-safety.py 拦截矩阵

| 触发条件 | 拦截内容 | 放行内容 |
|----------|----------|----------|
| Bash 中 hcloud csms download-secret | ✅ 拦截 | grep/cat 只读工具提及 API 名 |
| Bash 中 SDK 的 ShowSecretValue / DownloadSecret | ✅ 拦截 | 非 DEW/CSMS 服务 |
| MCP 结构化调用 DEW + 读密钥操作 | ✅ 拦截 | 非密钥读取操作 |
| 内联解释器脚本含密钥 SDK 调用 | ✅ 拦截 | — |

\\\python
# secret-safety.py 核心匹配模式 (模仿 aws-core)
DEW_PATTERN = re.compile(
    r'hcloud\\s+csms\\s+(download-secret|show-secret)'
    r'|hcloud\\s+kms\\s+decrypt'
    r'|\\.\\s*(download|show|get)[-_]?secret\\s*\\('
    r'|(Download|Show|Get)Secret(Value|Command)?\\s*\\(',
    re.I
)
\\\

---



---

## 全局规则文件 (新增)

> 对标 aws 仓库 rules/ 目录，Agent 加载插件时自动读取

# Huawei Cloud Agent Rules

> 对标 aws agent-toolkit-for-aws/rules/ 目录
> 作用: Agent 加载插件时自动读取，定义全局行为约束

---

## Core Principles

- **Always use the Huawei MCP server first** (sandboxed, observable, auditable). Fall back to hcloud CLI only when MCP is unavailable.
- **Before any task, check for available skills.** Use retrieve_skill or search_docs via MCP. Follow skill procedures over general knowledge.
- **When uncertain** about API parameters, quotas, limits, or error codes — search documentation via MCP. Do not guess.
- **Prefer IaC over direct CLI calls** for infrastructure creation (Terraform for Huawei Cloud, or hcloud CloudFormation equivalent).
- **Never use em dashes in resource names/tags.** Use hyphens only.
- **Match user language.** Respond in the same language the user writes in.

## Secret Safety

- **MUST load the huawei-dew skill** first for any secret, credential, API key, token, or password task.
- **MUST NOT call** hcloud csms download-secret or hcloud csms show-secret directly.
- **MUST NOT call** hcloud kms decrypt directly.
- **MUST use** {{resolve:csms:secret-id:SecretString:key}} with the MCP proxy — secrets resolve at runtime, never enter agent context.
- **NEVER** echo AK/SK values in conversation or logs.

## IAM Security

- **Least privilege by default.** Start with no permissions, add only what is needed.
- **MUST NOT create IAM users** — use IAM Identity Center (OneAccess) or temporary credentials.
- **MUST NOT create long-term AK/SK** — use temporary STS tokens.
- **MUST scope resource ARNs** — no * wildcards.
- **Confused deputy protection:** Add g:SourceAccount and g:SourceUrn conditions to trust policies.

## Network Security

- **MUST block public access by default** on OBS buckets.
- **MUST use security groups** (not iptables) for ECS/CCE access control.
- **SHOULD use VPC endpoints** for internal service access (no public internet).
- **MUST enable HTTPS-only** on OBS buckets and API endpoints.

## Observability

- **SHOULD enable CTS audit logging** on all accounts.
- **SHOULD configure Cloud Eye alarms** for production workloads (CPU > 80%, memory > 85%, disk > 90%).
- **SHOULD enable OBS access logging** for audit compliance.

## Cost Awareness

- **Before creating resources**, inform the user of estimated cost.
- **SHOULD suggest billing budgets** for production accounts.
- **SHOULD recommend CBR backup** for stateful resources (RDS, ECS system disks).

## Naming Conventions

- Resource names: lowercase, hyphens, max 64 chars
- Tags: include Environment (dev/staging/prod), Owner, Project
- OBS bucket names: globally unique, lowercase, no underscores

## MCP Usage Rules

- **Prefer MCP for ALL AWS-equivalent operations** — sandbox, audit, enterprise controls.
- **When MCP is unavailable:** Fall back to hcloud CLI. State clearly: "MCP unavailable, using local hcloud CLI."
- **run_script:** Use MCP run_script for deterministic calculations (cost math, data processing). Never do arithmetic in LLM reasoning.

## Skill Discovery Priority

1. **huawei-service-selector** — always check first for routing
2. **Service-specific skills** — load after routing
3. **Knowledge cards** (assets/) — quick facts without full skill load

## Red Flags — STOP and Re-evaluate

- Creating resources without user confirmation
- Using hcloud csms download-secret or kms decrypt directly
- Guessing API parameters without searching docs
- Creating IAM users or long-term AK/SK
- Opening OBS buckets to public access
- Skipping CTS/Cloud Eye on production workloads


## 五、Marketplace 分发体系

### .agents/plugins/marketplace.json

```json
{
  "name": "agent-toolkit-for-huawei",
  "interface": {
    "displayName": "Agent Toolkit for Huawei Cloud"
  },
  "plugins": [
    {
      "name": "huawei-core",
      "source": { "source": "local", "path": "./plugins/huawei-core" },
      "policy": { "installation": "AVAILABLE", "authentication": "ON_INSTALL" },
      "category": "Cloud"
    }
  ]
}
```

> **当前阶段仅 huawei-core 一个插件。** 后续可按需扩展 huawei-ai、huawei-data-analytics、huawei-devsecops。
